/**
 * This function creates a random number used when a queue can't be defined
 *
 * @return retunrs a random number
 *
 **/
 
function randomNum () {
  return Math.random().toString().split(".")[1].substring(0,8);
}

module.exports = {randomNum};